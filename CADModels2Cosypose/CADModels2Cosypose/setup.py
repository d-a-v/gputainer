from setuptools import setup, find_packages

setup(
    name='cad2cosypose',
    version='1.0.0',
    description='Automatically generate photorealistic images from BlenderProc and train CosyPose on them.',
    packages=find_packages(),
)
